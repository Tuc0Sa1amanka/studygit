#ifndef MATRIX_HPP
#define MATRIX_HPP
#include <cstddef>
int ** nums (size_t m, size_t n);
void input (int ** t, size_t m, size_t n);
void out (const int * const * t,size_t m, size_t n);
void dest (int ** t, size_t m);
#endif
